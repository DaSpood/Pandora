interface SimulatorConfig {
    // TODO
    // simulator mode
    // associated loot table file
    // unit price of each lootbox type (?)
    // for "budget" mode, how many of each box to open
    // for "until" mode, which lootbox is "purchased"
    // for "until" mode, stopping condition
    // for "until" mode, settings for multi-threaded big-number runs
    // ...
}
